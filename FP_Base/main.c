#include "docente.h"

#include <stdio.h>

int main() {
    Lista* lista;

    //inicializaLista(&lista);
    generateSeed();
    //crearTodasLasCartas(&lista);
    //imprimirInicio(&lista);
    //imprimirListaCartas(&lista);


    return 0;
}